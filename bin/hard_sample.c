void function(int para) {
    return 0;
}

struct test {
    int tmp;
};

int main() {
    int i, _j;//This is a comment
    /*
     * This
     * is
     * a
     * multi-line
     * 注释
     */
    size_t size;
    struct test t;
    const char* s = "he110\" w0r1d";
    char c = 'hello';//中文
    错误
    const char* chinese = "字符串"
    for (int i = 0; i < 10; i++) {
        function(i);
        if (i == 5) break;
    }
    size <<= 1;
    _j ||= 5;
    if (size != _j) {
        t.tmp++;
    }
    return 0;
}
